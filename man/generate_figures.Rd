% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figures.R
\name{generate_figures}
\alias{generate_figures}
\title{Generate the standard 6-figure bundle and write to disk}
\usage{
generate_figures(
  enriched,
  ranking,
  tissue,
  species_result = NULL,
  output_dir,
  formats = c("pdf", "png"),
  with_reference = TRUE,
  sites = NULL,
  variants = c("primary", "both")
)
}
\arguments{
\item{enriched}{Output of \code{\link{enrich_annotations}}.}

\item{ranking}{Output of \code{\link{rank_sirna}}.}

\item{tissue}{Output of \code{\link{tissue_safety}}.}

\item{species_result}{Output of \code{\link{species_match}}.}

\item{output_dir}{Directory to write to (created if absent).}

\item{formats}{Character vector of formats. Default \code{c("pdf","png")}.}

\item{with_reference}{Logical. If \code{TRUE} (default), additional
comparison figures using the built-in \code{\link{reference_set}}
are written: \code{01b_ranking_vs_reference} (violin),
\code{04b_species_vs_reference} (boxplot per species),
\code{04c_species_percentile} (percentile bars).}

\item{sites}{Optional. The CORE table from \code{\link{annotate_sites}},
which still carries \code{n_mismatch}. Supplying it adds the
mismatch-distribution figures; \code{enriched} alone cannot produce them
because \code{\link{enrich_annotations}} drops that column.}

\item{variants}{Either \code{"primary"} (default; one figure per data
view) or \code{"both"}, which additionally writes the alternative
rendering of each: cumulative mismatch curve (\code{07b}), percentage
composition (\code{08b}) and ortholog coverage (\code{09b}).}
}
\value{
Invisibly, a named list of file paths written.
}
\description{
Convenience wrapper that runs all six figure helpers and writes both
PDF and PNG versions to \code{output_dir}.
}
\examples{
\dontrun{
generate_figures(enriched, ranking, tissue, species_result,
                 output_dir = "results/figures")
}
}
